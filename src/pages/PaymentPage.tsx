import {parseEther,parseUnits,ZeroAddress} from 'ethers';import {AlertTriangle,ArrowLeft,LockKeyhole,Store} from 'lucide-react';import {useEffect,useState} from 'react';import {Link,useNavigate,useParams} from 'react-router-dom';import {useAccount,usePublicClient,useWalletClient} from 'wagmi';import {getInvoice} from '../api/invoices';import {preparePayment,submitPayment} from '../api/payments';import {ApiError} from '../api/client';import {ConnectWalletButton} from '../components/ConnectWalletButton';import {ErrorState,LoadingState} from '../components/States';import {PaymentSummaryCard} from '../components/PaymentSummaryCard';import {TokenSelector} from '../components/TokenSelector';import {chainId,contractAddress,isMockMode,usdcAddress} from '../config/blockchain';import {sikaPayAbi,usdcAbi} from '../config/contracts';import {mockStore} from '../mocks/store';import type {Invoice,Payment,SupportedToken} from '../types';import {maskPhone,money,networkName} from '../utils';
const walletError=(error:unknown)=>{if(error instanceof ApiError){const messages:Record<string,string>={INVOICE_EXPIRED:'This invoice expired before verification completed.',INVOICE_ALREADY_PAID:'This invoice has already been paid.',UNDERPAYMENT:'The confirmed transaction did not cover the invoice amount.',PAYMENT_EVENT_NOT_FOUND:'The transaction does not contain the expected SikaPay payment.',TRANSACTION_REVERTED:'The blockchain transaction was reverted.',UNSUPPORTED_PAYMENT_TOKEN:'The payment used an unsupported token.',RPC_NOT_CONFIGURED:'Backend blockchain verification is not configured.'};return messages[error.code]||error.message}const message=error instanceof Error?error.message:'';if(/rejected|denied/i.test(message))return 'Payment was rejected in your wallet. No funds were sent.';if(/insufficient/i.test(message))return 'Your wallet does not have enough balance for this payment and network fee.';if(/chain|network/i.test(message))return 'Please switch your wallet to Ethereum Sepolia and try again.';return 'The blockchain transaction failed. No Mobile Money payout was started.'};
export function PaymentPage(){const {invoiceId}=useParams(),navigate=useNavigate(),{address,isConnected,chain}=useAccount(),{data:wallet}=useWalletClient(),publicClient=usePublicClient();const [invoice,setInvoice]=useState<Invoice>(),[loading,setLoading]=useState(true),[error,setError]=useState(''),[token,setToken]=useState<SupportedToken>('ETH'),[submitting,setSubmitting]=useState(false),[walletState,setWalletState]=useState('');const load=()=>{setLoading(true);setError('');getInvoice(invoiceId||'').then(setInvoice).catch(e=>setError(e instanceof Error?e.message:'Invoice unavailable')).finally(()=>setLoading(false))};useEffect(load,[invoiceId]);if(loading)return <div className="shell py-12"><LoadingState label="Fetching secure invoice…"/></div>;if(error&&!invoice)return <div className="shell py-12"><ErrorState message={error} onRetry={load}/></div>;if(!invoice)return <div className="shell py-12"><ErrorState message="This invoice is invalid."/></div>;const expired=new Date(invoice.expiresAt)<new Date(),paid=invoice.status==='COMPLETED',wrongNetwork=isConnected&&chain?.id!==chainId;
const confirm=async()=>{if(expired||paid||submitting)return;setSubmitting(true);setError('');try{const prepared=await preparePayment(invoice,token);const amount=token==='ETH'?parseEther(prepared.amountCrypto):parseUnits(prepared.amountCrypto,6);if(isMockMode){const payment:Payment={transactionId:prepared.transactionId,invoiceId:invoice.id,status:'BLOCKCHAIN_CONFIRMING',blockchainStatus:'CONFIRMING',payoutStatus:'PENDING',amountCrypto:prepared.amountCrypto,token,amountGhs:invoice.amountGhs,customerWallet:address||'0xDemo9E45b8847d8322A5fA52A5f4D9C9b2a8',createdAt:new Date().toISOString(),merchantName:invoice.merchantName};mockStore.savePayment(payment);navigate(`/transaction/${prepared.transactionId}`);return}if(!wallet||!publicClient||!address)throw new Error('Wallet is not connected');if(chain?.id!==chainId)throw new Error('Wrong network');if(!invoice.onchainId)throw new Error('Invoice is missing its onchain identifier');if(contractAddress===ZeroAddress)throw new Error('SikaPay contract is not configured');let txHash:`0x${string}`;
if(token==='USDC'){if(usdcAddress===ZeroAddress)throw new Error('USDC token is not configured');setWalletState('Approve USDC in your wallet');const approval=await wallet.writeContract({account:address,chain,address:usdcAddress,abi:usdcAbi,functionName:'approve',args:[contractAddress,amount]});setWalletState('Confirming USDC approval');const approvalReceipt=await publicClient.waitForTransactionReceipt({hash:approval});if(approvalReceipt.status!=='success')throw new Error('USDC approval reverted');setWalletState('Approve the SikaPay payment');txHash=await wallet.writeContract({account:address,chain,address:contractAddress,abi:sikaPayAbi,functionName:'payInvoice',args:[invoice.onchainId,usdcAddress,amount]})}else{setWalletState('Approve the ETH payment');txHash=await wallet.writeContract({account:address,chain,address:contractAddress,abi:sikaPayAbi,functionName:'payInvoice',args:[invoice.onchainId,ZeroAddress,amount],value:amount})}
setWalletState('Confirming on Ethereum Sepolia');const receipt=await publicClient.waitForTransactionReceipt({hash:txHash,confirmations:1});if(receipt.status!=='success')throw new Error('Transaction reverted');setWalletState('SikaPay is verifying the payment');const confirmed=await submitPayment({invoiceReference:invoice.reference||invoice.id,txHash,senderAddress:address,asset:token,cryptoAmount:prepared.amountCrypto});navigate(`/transaction/${confirmed.transactionId}`)}catch(e){setError(walletError(e));setSubmitting(false);setWalletState('')}};
return <div className="shell py-8"><Link to="/pay" className="mb-6 inline-flex items-center gap-2 text-sm font-bold text-forest"><ArrowLeft size={17}/>Choose another invoice</Link><div className="mx-auto grid max-w-5xl gap-6 lg:grid-cols-[1fr_.8fr]"><div className="space-y-5"><div className="card p-6 sm:p-8"><div className="flex items-start justify-between"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-forest text-white"><Store/></span><span className="rounded-full bg-emerald/10 px-3 py-1 text-xs font-bold text-emerald">{invoice.reference||invoice.id}</span></div><p className="mt-7 text-sm text-[#66766e]">Paying</p><h1 className="mt-1 text-3xl font-extrabold">{invoice.merchantName}</h1><p className="mt-2 text-[#66766e]">{invoice.description}</p><div className="my-6 border-t border-black/5"/><p className="text-sm text-[#66766e]">Amount requested</p><p className="amount mt-1 text-4xl font-extrabold text-forest">{money(invoice.amountGhs)}</p><p className="mt-5 rounded-2xl bg-[#e9f5ed] p-4 text-sm font-semibold leading-6 text-forest">{invoice.merchantName} will receive {money(invoice.amountGhs)} directly through {networkName(invoice.network)}.</p><div className="mt-4 flex justify-between text-xs text-[#718078]"><span>Expires {new Date(invoice.expiresAt).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'})}</span><span>{maskPhone(invoice.merchantPhone)}</span></div></div><div className="card p-6"><label className="label">Choose how to pay</label><TokenSelector value={token} onChange={setToken}/></div></div><div className="space-y-5"><PaymentSummaryCard invoice={invoice} token={token}/><div className="card p-6"><ConnectWalletButton/>{isMockMode&&!isConnected&&<p className="mt-3 text-center text-xs text-[#68776f]">No wallet installed? The demo payment can still continue securely.</p>}<button onClick={confirm} disabled={expired||paid||submitting||wrongNetwork||(!isConnected&&!isMockMode)} className="btn-primary mt-3 w-full">{walletState||(submitting?'Preparing transaction…':expired?'Invoice expired':paid?'Already paid':'Confirm payment')}</button><p className="mt-4 flex items-center justify-center gap-1.5 text-center text-xs text-[#75837c]"><LockKeyhole size={13}/>Success appears only after backend verification.</p>{error&&<p role="alert" className="mt-3 flex gap-2 rounded-xl bg-red-50 p-3 text-sm text-red-700"><AlertTriangle size={17}/>{error}</p>}</div></div></div></div>}
