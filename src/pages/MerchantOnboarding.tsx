import {ArrowRight,Building2,Smartphone} from 'lucide-react';import {FormEvent,useState} from 'react';import {useNavigate} from 'react-router-dom';import {MerchantAuthLayout} from '../components/MerchantAuthLayout';import {useAuth} from '../hooks/useAuth';
export function MerchantOnboarding(){const {completeOnboarding}=useAuth(),navigate=useNavigate(),[loading,setLoading]=useState(false),[error,setError]=useState('');const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();setError('');const fd=new FormData(e.currentTarget),businessName=String(fd.get('businessName')).trim(),momoPhone=String(fd.get('momoPhone')).trim();if(businessName.length<2||!/^\+233\d{9}$/.test(momoPhone)){setError('Enter a business name and a Ghana number such as +233241234567.');return}setLoading(true);try{await completeOnboarding({businessName,momoPhone});navigate('/merchant',{replace:true})}catch(err){setError(err instanceof Error?err.message:'Unable to save merchant profile.')}finally{setLoading(false)}};return <MerchantAuthLayout title="Complete your business profile" subtitle="Add the details SikaPay will use to create invoices and settle Mobile Money payouts."><form onSubmit={submit} className="space-y-5"><div><label className="label" htmlFor="onboard-business">Business name</label><div className="relative"><Building2 className="absolute left-4 top-4 text-[#829087]" size={18}/><input className="input !pl-11" id="onboard-business" name="businessName" required/></div></div><div><label className="label" htmlFor="onboard-phone">MTN Mobile Money number</label><div className="relative"><Smartphone className="absolute left-4 top-4 text-[#829087]" size={18}/><input className="input !pl-11" id="onboard-phone" name="momoPhone" placeholder="+233241234567" required/></div></div>{error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="btn-primary w-full">{loading?'Saving profile…':'Continue to dashboard'}<ArrowRight size={18}/></button></form></MerchantAuthLayout>}
