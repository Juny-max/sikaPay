import {ArrowLeft,CheckCircle2,Mail} from 'lucide-react';import {FormEvent,useState} from 'react';import {Link} from 'react-router-dom';import {MerchantAuthLayout} from '../components/MerchantAuthLayout';import {useAuth} from '../hooks/useAuth';import {emailPattern} from '../utils/authValidation';
export function ForgotPassword(){const {requestReset}=useAuth(),[email,setEmail]=useState(''),[loading,setLoading]=useState(false),[sent,setSent]=useState(false),[error,setError]=useState('');const submit=async(e:FormEvent)=>{e.preventDefault();setError('');if(!emailPattern.test(email)){setError('Enter a valid email address.');return}setLoading(true);try{await requestReset(email);setSent(true)}catch(err){setError(err instanceof Error?err.message:'Unable to send reset link.')}finally{setLoading(false)}};return <MerchantAuthLayout title="Reset your password" subtitle="Enter your account email and we’ll send password reset instructions.">{sent?<div className="card border-emerald/15 p-7 text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-full bg-emerald/10 text-emerald"><CheckCircle2/></span><h2 className="mt-5 text-xl font-extrabold">Check your inbox</h2><p className="mt-2 leading-7 text-[#65756d]">If an account exists for <strong>{email}</strong>, reset instructions have been sent.</p><Link to="/merchant/signin" className="btn-primary mt-6">Return to sign in</Link></div>:<form onSubmit={submit} className="space-y-5"><div><label className="label" htmlFor="reset-email">Email address</label><div className="relative"><Mail className="absolute left-4 top-4 text-[#88958f]" size={18}/><input value={email} onChange={e=>setEmail(e.target.value)} className="input !pl-11" id="reset-email" type="email" autoComplete="email" placeholder="you@business.com" required/></div></div>{error&&<p role="alert" className="rounded-xl bg-red-50 p-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="btn-primary w-full">{loading?'Sending…':'Send reset link'}</button><Link to="/merchant/signin" className="flex items-center justify-center gap-2 text-sm font-bold text-forest"><ArrowLeft size={16}/>Back to sign in</Link></form>}</MerchantAuthLayout>}
