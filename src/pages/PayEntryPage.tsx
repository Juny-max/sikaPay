import {ArrowRight,Keyboard,ScanLine,Sparkles} from 'lucide-react';import {FormEvent} from 'react';import {useNavigate} from 'react-router-dom';import {QRScanner} from '../components/QRScanner';export function PayEntryPage(){const nav=useNavigate();const submit=(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const id=String(new FormData(e.currentTarget).get('invoice')).trim();if(id)nav(`/pay/${id}`)};return <div className="shell py-12"><div className="mx-auto max-w-xl"><div className="text-center"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-emerald/10 text-emerald"><ScanLine size={28}/></span><p className="eyebrow mt-5">Pay with SikaPay</p><h1 className="mt-2 text-4xl font-extrabold">Scan. Pay. Done.</h1><p className="mt-3 text-[#64736c]">Scan a merchant’s QR code or enter the invoice code.</p></div><div className="card mt-8 p-6 sm:p-8"><h2 className="mb-4 flex items-center gap-2 font-extrabold"><ScanLine size={19} className="text-emerald"/>Scan merchant QR</h2><QRScanner/><div className="my-7 flex items-center gap-3"><span className="h-px flex-1 bg-black/10"/><span className="text-xs font-bold uppercase text-[#819088]">or use a code</span><span className="h-px flex-1 bg-black/10"/></div><form onSubmit={submit}><label htmlFor="invoice" className="label flex items-center gap-2"><Keyboard size={17}/>Invoice code</label><div className="flex gap-2"><input id="invoice" name="invoice" className="input uppercase" placeholder="e.g. INV-001"/><button className="btn-primary !rounded-2xl !px-4" aria-label="Open invoice"><ArrowRight/></button></div></form></div><button onClick={()=>nav('/pay/INV-DEMO')} className="btn-secondary mt-4 w-full"><Sparkles size={18}/>Load demo invoice</button></div></div>}
