import {ArrowRight,CheckCircle2,ExternalLink,ReceiptText} from 'lucide-react';import {useEffect,useState} from 'react';import {Link,useParams} from 'react-router-dom';import {getPayment} from '../api/payments';import {TransactionProgress} from '../components/TransactionProgress';import {ErrorState,LoadingState} from '../components/States';import {SuccessModal} from '../components/SuccessModal';import {explorerUrl,isMockMode} from '../config/blockchain';import {demoInvoice} from '../mocks/data';import {mockStore} from '../mocks/store';import type {Payment,PaymentStatus} from '../types';import {money,short} from '../utils';export function TransactionPage(){const {transactionId}=useParams();const [payment,setPayment]=useState<Payment>(),[error,setError]=useState(''),[showSuccess,setShowSuccess]=useState(false);useEffect(()=>{let mounted=true;const poll=async()=>{try{let p=await getPayment(transactionId||'');if(!p)throw new Error('Transaction not found');if(isMockMode&&p.status!=='COMPLETED'&&p.status!=='FAILED'){const elapsed=Date.now()-new Date(p.createdAt).getTime();let status:PaymentStatus='BLOCKCHAIN_CONFIRMING';if(elapsed>2200)status='CRYPTO_CONFIRMED';if(elapsed>4300)status='PROCESSING_MOMO';if(elapsed>7000)status='COMPLETED';p={...p,status,blockchainStatus:elapsed>2200?'CONFIRMED':'CONFIRMING',payoutStatus:elapsed>4300?(elapsed>7000?'SUCCESS':'PROCESSING'):'PENDING',txHash:elapsed>2200?'0x71a20f352774005eb99f671ca9d2875be967ba87a6f3419af08c5d0c1a22abcd':undefined,momoReference:elapsed>7000?`SP-${new Date().getFullYear()}-${String(Date.now()).slice(-5)}`:undefined};mockStore.savePayment(p)}if(mounted){setPayment(p);if(p.status==='COMPLETED')setShowSuccess(true)}}catch(e){if(mounted)setError(e instanceof Error?e.message:'Unable to load payment')}};void poll();const timer=setInterval(poll,isMockMode?1000:3000);return()=>{mounted=false;clearInterval(timer)}},[transactionId]);if(error)return <div className="shell py-12"><ErrorState message={error}/></div>;if(!payment)return <div className="shell py-12"><LoadingState label="Locating your payment…"/></div>;const complete=payment.status==='COMPLETED';return <div className="shell py-10"><div className="mx-auto max-w-3xl"><div className="text-center"><span className={`mx-auto grid h-14 w-14 place-items-center rounded-full ${complete?'bg-emerald text-white':'bg-emerald/10 text-emerald'}`}>{complete?<CheckCircle2/>:<ReceiptText/>}</span><p className="eyebrow mt-5">Transaction {payment.transactionId}</p><h1 className="mt-2 text-3xl font-extrabold">{complete?'Payment complete':'Your payment is moving'}</h1><p className="mt-2 text-[#66766e]">{complete?'The Mobile Money payout was successful.':'Keep this page open while we confirm and settle your payment.'}</p></div><div className="mt-8 grid gap-5 md:grid-cols-[1.1fr_.9fr]"><div className="card p-7"><h2 className="mb-7 text-xl font-extrabold">Payment progress</h2><TransactionProgress status={payment.status}/></div><div className="card h-fit p-6"><h2 className="text-lg font-extrabold">Summary</h2><div className="mt-4 divide-y divide-black/5">{[['Paid',`${payment.amountCrypto} ${payment.token}`],['Merchant gets',money(payment.amountGhs)],['Wallet',short(payment.customerWallet)],['Transaction',short(payment.txHash)]].map(([a,b])=><div className="flex justify-between gap-3 py-3 text-sm" key={a}><span className="text-[#66766e]">{a}</span><span className="font-bold">{b}</span></div>)}</div>{payment.txHash&&<a href={`${explorerUrl}${payment.txHash}`} target="_blank" rel="noreferrer" className="btn-secondary mt-4 w-full">Etherscan <ExternalLink size={16}/></a>}</div></div>{complete&&<Link to="/merchant" className="btn-primary mx-auto mt-6 flex w-fit">View merchant dashboard <ArrowRight size={17}/></Link>}</div>{showSuccess&&<SuccessModal payment={payment} phone={mockStore.getInvoice(payment.invoiceId)?.merchantPhone||demoInvoice.merchantPhone} onClose={()=>setShowSuccess(false)}/>}</div>}
