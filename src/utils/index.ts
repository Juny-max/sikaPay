import type {MobileMoneyNetwork} from '../types';export const money=(v:number)=>new Intl.NumberFormat('en-GH',{style:'currency',currency:'GHS'}).format(v).replace('GHS','GH₵');export const short=(v?:string,n=5)=>v?`${v.slice(0,n+2)}…${v.slice(-4)}`:'—';export const maskPhone=(v:string)=>`${v.slice(0,3)}****${v.slice(-3)}`;export const networkName=(n:MobileMoneyNetwork)=>({MTN:'MTN Mobile Money',TELECEL:'Telecel Cash',AT:'AT Money'}[n]);export const relativeDate=(d:string)=>new Intl.DateTimeFormat('en-GH',{month:'short',day:'numeric',hour:'2-digit',minute:'2-digit'}).format(new Date(d));
