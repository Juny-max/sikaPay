import {Banknote,CheckCircle2,ReceiptText} from 'lucide-react';import {money} from '../utils';export function MerchantStats({total,count}:{total:number;count:number}){const data=[[Banknote,'Payments today',money(total)],[ReceiptText,'Total received',money(total)],[CheckCircle2,'Successful',String(count)]] as const;return <div className="grid gap-3 sm:grid-cols-3">{data.map(([Icon,label,value])=><div key={label} className="card p-5"><span className="mb-5 grid h-10 w-10 place-items-center rounded-xl bg-emerald/10 text-emerald"><Icon size={20}/></span><p className="text-sm font-medium text-[#63736b]">{label}</p><p className="amount mt-1 text-2xl font-extrabold text-forest">{value}</p></div>)}</div>}
