import {ConnectButton} from '@rainbow-me/rainbowkit';
import {AlertTriangle,ChevronDown,ExternalLink,Wallet} from 'lucide-react';

const hasWalletConnect=Boolean(import.meta.env.VITE_WALLETCONNECT_PROJECT_ID?.trim());
const hasInjectedWallet=()=>typeof window!=='undefined'&&Boolean(window.ethereum);

export function ConnectWalletButton(){return <ConnectButton.Custom>{({account,chain,openAccountModal,openChainModal,openConnectModal,mounted})=>{const connected=mounted&&account&&chain;if(mounted&&!connected&&!hasWalletConnect&&!hasInjectedWallet())return <div className="rounded-2xl border border-amber-200 bg-amber-50 p-4"><div className="flex gap-3"><AlertTriangle className="mt-0.5 shrink-0 text-amber-600" size={19}/><div><p className="text-sm font-extrabold text-amber-900">Mobile wallet connection needs WalletConnect</p><p className="mt-1 text-xs leading-5 text-amber-800">Open this page inside your wallet’s browser, or configure a Reown project ID to choose MetaMask, Trust Wallet, Rainbow and other mobile wallets.</p><a href="https://cloud.reown.com" target="_blank" rel="noreferrer" className="mt-3 inline-flex items-center gap-1 text-xs font-extrabold text-amber-900 underline">Configure Reown <ExternalLink size={13}/></a></div></div></div>;return <div aria-hidden={!mounted} className={!mounted?'opacity-0':''}>{!connected?<button className="btn-primary w-full" onClick={openConnectModal}><Wallet size={18}/>Connect wallet</button>:chain.unsupported?<button className="btn-primary w-full !bg-red-600" onClick={openChainModal}><AlertTriangle size={18}/>Switch to Sepolia</button>:<button className="btn-secondary w-full" onClick={openAccountModal}><span className="h-2 w-2 rounded-full bg-emerald"/>{account.displayName}<ChevronDown size={15}/></button>}</div>}}</ConnectButton.Custom>}
