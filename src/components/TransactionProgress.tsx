import {Check,LoaderCircle} from 'lucide-react';import type {PaymentStatus} from '../types';const steps=['Payment submitted','Ethereum confirmation','SikaPay bridge processing','Mobile Money payout initiated','Merchant paid'];const indexes:Record<PaymentStatus,number>={AWAITING_PAYMENT:-1,BLOCKCHAIN_CONFIRMING:1,CRYPTO_CONFIRMED:2,PROCESSING_MOMO:3,COMPLETED:5,FAILED:0};export function TransactionProgress({status}:{status:PaymentStatus}){const current=indexes[status];return <ol>{steps.map((s,i)=>{const done=i<current,active=i===current;return <li key={s} className="relative flex gap-4 pb-7 last:pb-0"><div className={`relative z-10 grid h-8 w-8 shrink-0 place-items-center rounded-full border-2 ${done?'border-emerald bg-emerald text-white':active?'border-emerald bg-white text-emerald':'border-[#d9e1dc] bg-white text-[#9aa69f]'}`}>{done?<Check size={16}/>:active?<LoaderCircle size={16} className="animate-spin"/>:<span className="text-xs font-bold">{i+1}</span>}</div>{i<4&&<span className={`absolute left-[15px] top-8 h-[calc(100%-2rem)] w-0.5 ${done?'bg-emerald':'bg-[#d9e1dc]'}`}/>}<div className="pt-1"><p className={`font-bold ${done||active?'text-forest':'text-[#8b9992]'}`}>{s}</p>{active&&<p className="mt-1 text-sm text-[#607168]">This usually takes just a moment.</p>}</div></li>})}</ol>}
