import {Home,LayoutDashboard,ScanLine} from 'lucide-react';import {NavLink} from 'react-router-dom';export function MobileNavigation(){return <nav className="fixed bottom-3 left-1/2 z-40 flex -translate-x-1/2 gap-1 rounded-full border border-white/60 bg-forest/95 p-1.5 text-white shadow-2xl backdrop-blur md:hidden">{[[Home,'/','Home'],[ScanLine,'/pay','Pay'],[LayoutDashboard,'/merchant','Merchant']].map(([Icon,to,label])=><NavLink key={String(to)} to={String(to)} aria-label={String(label)} className={({isActive})=>`grid h-12 w-16 place-items-center rounded-full ${isActive?'bg-white text-forest':'text-white/70'}`}><Icon size={20}/></NavLink>)}</nav>}
