import type {SupportedToken} from '../types';export function TokenSelector({value,onChange}:{value:SupportedToken;onChange:(v:SupportedToken)=>void}){return <div className="grid grid-cols-2 gap-2 rounded-2xl bg-[#eff3ef] p-1.5" role="radiogroup" aria-label="Payment token">{(['ETH','USDC'] as const).map(t=><button key={t} role="radio" aria-checked={value===t} onClick={()=>onChange(t)} className={`flex items-center justify-center gap-2 rounded-xl p-3 font-bold ${value===t?'bg-white text-forest shadow-sm':'text-[#64736c]'}`}><span className={`grid h-7 w-7 place-items-center rounded-full text-xs ${t==='ETH'?'bg-[#627eea] text-white':'bg-[#2775ca] text-white'}`}>{t[0]}</span>{t}</button>)}</div>}
