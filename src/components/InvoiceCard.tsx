import {Clock3,Store} from 'lucide-react';import type {Invoice} from '../types';import {money,networkName} from '../utils';import {PaymentStatusBadge} from './PaymentStatusBadge';export function InvoiceCard({invoice}:{invoice:Invoice}){return <div className="card overflow-hidden"><div className="bg-forest p-6 text-white"><div className="mb-5 flex items-center justify-between"><span className="rounded-full bg-white/10 px-3 py-1 text-xs font-bold">{invoice.id}</span><PaymentStatusBadge status={invoice.status}/></div><p className="text-sm text-white/65">Amount requested</p><p className="amount mt-1 text-4xl font-extrabold">{money(invoice.amountGhs)}</p></div><div className="space-y-4 p-6"><div className="flex gap-3"><Store className="mt-0.5 text-emerald" size={20}/><div><p className="font-bold">{invoice.merchantName}</p><p className="text-sm text-[#66766e]">{invoice.description} · {networkName(invoice.network)}</p></div></div><div className="flex gap-3"><Clock3 className="mt-0.5 text-emerald" size={20}/><div><p className="text-sm font-bold">Expires</p><p className="text-sm text-[#66766e]">{new Date(invoice.expiresAt).toLocaleString()}</p></div></div></div></div>}
