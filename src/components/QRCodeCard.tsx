import QRCode from 'react-qr-code';import {Check,Copy} from 'lucide-react';import {useState} from 'react';export function QRCodeCard({value}:{value:string}){const [copied,setCopied]=useState(false);const copy=async()=>{await navigator.clipboard.writeText(value);setCopied(true);setTimeout(()=>setCopied(false),1800)};return <div className="card p-6 text-center"><p className="eyebrow">Scan to pay</p><div className="mx-auto my-5 w-fit rounded-2xl border border-black/10 bg-white p-4"><QRCode value={value} size={180}/></div><div className="flex items-center gap-2 rounded-xl bg-[#f3f6f3] p-2 pl-3 text-left"><span className="min-w-0 flex-1 truncate text-xs text-[#596a61]">{value}</span><button onClick={copy} aria-label="Copy payment link" className="rounded-lg bg-white p-2 text-emerald shadow-sm">{copied?<Check size={16}/>:<Copy size={16}/>}</button></div></div>}
