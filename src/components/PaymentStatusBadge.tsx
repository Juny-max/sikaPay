import type {PaymentStatus} from '../types';const labels:Record<PaymentStatus,string>={AWAITING_PAYMENT:'Awaiting payment',BLOCKCHAIN_CONFIRMING:'Confirming',CRYPTO_CONFIRMED:'Crypto confirmed',PROCESSING_MOMO:'Processing MoMo',COMPLETED:'Completed',FAILED:'Failed'};export function PaymentStatusBadge({status}:{status:PaymentStatus}){const color=status==='COMPLETED'?'bg-emerald/10 text-emerald':status==='FAILED'?'bg-red-50 text-red-600':status==='AWAITING_PAYMENT'?'bg-amber-50 text-amber-700':'bg-blue-50 text-blue-700';return <span className={`inline-flex rounded-full px-3 py-1 text-xs font-bold ${color}`}>{labels[status]}</span>}
