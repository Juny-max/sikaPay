import {Menu,WalletCards,X} from 'lucide-react';import {motion} from 'motion/react';import {useState} from 'react';import {Link,NavLink} from 'react-router-dom';import {BrandMark} from './brand/BrandMark';import {navbarReveal} from './motion/variants';
export function Logo(){return <BrandMark mode="navbar"/>}
export function AppHeader({externalBrand=false}:{externalBrand?:boolean}){const [open,setOpen]=useState(false);const links=[['/pay','Pay'],['/merchant','For merchants'],['/merchant/signin','Merchant sign in']];return <motion.header variants={navbarReveal} initial="hidden" animate="visible" className="sticky top-0 z-40 border-b border-black/5 bg-[#f6f8f5]/90 backdrop-blur-xl"><div className="shell flex h-20 items-center justify-between"><div data-navbar-brand-target className="flex h-10 min-w-[124px] items-center">{!externalBrand&&<Logo/>}</div><motion.nav initial={{opacity:0,y:-8}} animate={{opacity:1,y:0}} transition={{delay:.18,duration:.45}} className="hidden items-center gap-7 md:flex">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`text-sm font-bold ${isActive?'text-emerald':'text-[#506159] hover:text-forest'}`}>{label}</NavLink>)}<Link to="/pay" className="btn-primary !px-4 !py-2.5"><WalletCards size={17}/>Make payment</Link></motion.nav><button aria-label="Toggle menu" className="md:hidden" onClick={()=>setOpen(!open)}>{open?<X/>:<Menu/>}</button></div>{open&&<nav className="shell flex flex-col gap-3 border-t border-black/5 py-4 md:hidden">{links.map(([to,label])=><Link key={to} to={to} onClick={()=>setOpen(false)} className="rounded-xl px-3 py-2 font-bold hover:bg-white">{label}</Link>)}</nav>}</motion.header>}
