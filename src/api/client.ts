import {isMockMode} from '../config/blockchain'; export const apiBase=import.meta.env.VITE_API_BASE_URL||'http://localhost:5000/api'; export async function request<T>(path:string,init?:RequestInit):Promise<T>{if(isMockMode)throw new Error('mock');const res=await fetch(`${apiBase}${path}`,{...init,headers:{'Content-Type':'application/json',...init?.headers}});if(!res.ok)throw new Error(`Request failed (${res.status})`);return res.json() as Promise<T>}
